import { ErrorBoundary } from '@/core/error-boundary';

const App = () => {
  return (
    <ErrorBoundary>
      <>
        <h1>Start React TypeScript Vite template</h1>
      </>
    </ErrorBoundary>
  );
};

export default App;
